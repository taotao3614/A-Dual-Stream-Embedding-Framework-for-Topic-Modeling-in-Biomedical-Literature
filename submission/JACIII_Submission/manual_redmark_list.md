# 需手动标红的浮动环境清单(table / figure / algorithm)

> 由于 `fujipressarticle` 类的 `\caption` 强制黑色 + 浮动延迟排版,
> 工具自动加的颜色组在 PDF 里不会显示。下面列出所有 caption / 内容
> 实际改动过的浮动,需要在最终 PDF 里手动标红。

## 标红方式建议
- **caption 改写**:用 PDF 编辑器把 caption 文字标红,或在 .tex 里临时把 `\caption{X}` 改成 `\caption{\textcolor{red}{X}}`
- **表格数据修正**:在 PDF 里把改动的 cell 文字标红
- **图像本身**:无法在 PDF 里"标红"(光栅/矢量内容),在回复信里说明即可

---

## 表格(Tables)

### Table 1 — Methodology 4.x 节,4 个 dual-stream 模型组合
- **caption 改写** → R1.10
  - 旧:`The dual-stream embedding model combinations.` (短)
  - 新:`The four dual-stream embedding model combinations evaluated in this study, showing the transformer encoder assigned to the text stream and to the MeSH stream in each configuration.`
- **数据列错乱修正** → R2.6(相关):Model name 列原与 Table 2 不一致,已对齐
- PDF 搜索锚句:`The four dual-stream embedding model combinations evaluated`

### Table 2 — Results / Model-Specific Performance,4 模型 TC/TD 性能
- **仅 caption 改写** → R1.10
  - 旧:`Summary of Performance` (4 个词)
  - 新:`Best topic coherence (TC), topic diversity (TD), and combined TC+TD scores for each dual-stream model, with the corresponding optimal (α, β) parameters and the relative improvement over the single-stream baseline (TC+TD = 1.2530). Each metric column reports the best value attained over the α-β grid, which may occur at different parameter settings.`
- **数据行未改动**
- PDF 搜索锚句:`Best topic coherence (TC), topic diversity (TD), and combined TC+TD scores for each dual-stream`

### Table 3 — Results / Enhanced Interpretability,LLM 标注示例
- **caption 改写** → R1.10
  - 旧:`Examples of LLM-generated topic titles`(简短)
  - 新:`Representative examples of human-readable topic titles generated by the Qwen-14B LLM from BERTopic keyword sets, illustrating the interpretability gain over raw keyword lists.`
- **行数从 10 行减为 5 行**(E 阶段精简)
- PDF 搜索锚句:`Representative examples of human-readable topic titles generated by the Qwen-14B LLM`

### Table 4 — Results / Ablation Study Results
- **caption 改写** → R1.10
  - 新:`Ablation study results: topic coherence (TC), topic diversity (TD), and combined TC+TD scores for the single-stream input configurations A1–A4 (concatenated inputs, no α–β fusion), compared with the baseline and the full dual-stream model with parameterized fusion.`
- **数据行未改动**
- PDF 搜索锚句:`Ablation study results: topic coherence (TC), topic diversity (TD), and combined TC+TD scores for the single-stream`

### Table 5 — Discussion / Comparison with Related Work(方法对比表)
- **未改动**(原本就存在,只是 R1.11 让在 Introduction 前向引用)
- 无需手动处理

---

## 图(Figures)

### Fig.1 — System workflow(系统流程图,Methodology / System Workflow Overview)
- **caption 仅删除 `\textbf{}` 重复加粗**,文字未变
- **非审稿意见**(JACIII 模板适配)
- 不需要标红

### Fig.2 — BERTopic pipeline architecture(Methodology / Topic Modelling Pipeline)
- **caption 未改动**(`Modular architecture of the BERTopic pipeline.`)
- 不需要标红

### Fig.3 — TC Heatmap(Results / Overall Experimental Outcomes)
- **caption 改写** → R1.10:`Heatmap of topic coherence (TC) scores across the α–β parameter grid under the Dual PubMedBERT configuration.`
- **图像重制为 PDF 矢量图 + 退化格剔除** → R1.9
- PDF 搜索锚句(caption):`Heatmap of topic coherence (TC) scores across the α-β parameter grid`

### Fig.4 — TD Heatmap(Results / Overall Experimental Outcomes)
- **caption 改写** → R1.10:`Heatmap of topic diversity (TD) scores across the α–β parameter grid under the Dual PubMedBERT configuration.`
- **R1.9** 矢量图 + 退化格剔除
- PDF 搜索锚句:`Heatmap of topic diversity (TD) scores`

### Fig.5 — Combined TC+TD Heatmap(Results / Overall Experimental Outcomes)
- **caption 改写** → R1.10
- **R1.9** 矢量图 + 退化格剔除
- PDF 搜索锚句:`Heatmap of combined topic coherence and diversity scores`

### Fig.6 — Model Comparison Bar Chart(Results / Model-Specific Performance)
- **caption 改写** → R1.10 + R1.9:`Performance comparison of dual-stream model variants across TC, TD, and combined scores. Dual PubMedBERT achieves the highest combined score (TC+TD = 1.3443) with optimal parameters α=0.45 and β=0.40. Data source: Table 2.`
- **R1.9** 矢量图
- PDF 搜索锚句:`Performance comparison of dual-stream model variants across TC, TD, and combined scores`

### Fig.7 — Ablation Comparison(Results / Ablation Study Results)
- **caption 改写** → R1.10 + R1.9:`Ablation study results showing the progressive performance variation across different input configurations (A1-A4) without parameterized fusion...`
- **R1.9** 矢量图
- PDF 搜索锚句:`Ablation study results showing the progressive performance variation across different input configurations`

---

## Algorithm 环境(如有)

### Algorithm 1, Algorithm 2(预处理 + Text-MeSH Fusion)
- 检查是否改动了算法步骤或 caption
- 若 caption 改了 → R1.10 同处理
- 数据未改 → 无需操作
